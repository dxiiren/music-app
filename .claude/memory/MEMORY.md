# Project Memory — music-app

Index of stored memories. One line per memory: `- [Title](file.md) — hook (when to read it)`.
Each memory lives in its own `*.md` file next to this index with frontmatter
(`name`, `description`, `metadata.type` = `reference` | `feedback` | `project`).

## Memories

_No memories yet._
